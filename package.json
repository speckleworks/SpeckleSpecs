{
  "name": "speckleoas",
  "version": "1.0.0",
  "description": "### Generate the spec from all the files: Install `multi-file-swagger`:",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "dependencies": {
    "swagger-vue": "^1.0.3",
    "widdershins": "^2.3.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-shins": "widdershins -y index-compiled.yaml -o specs.md",
    "build-swagger": "multi-file-swagger index.yaml > index.json && multi-file-swagger index.yaml > index-compiled.yaml -o yaml"
  },
  "author": "",
  "license": "ISC"
}
