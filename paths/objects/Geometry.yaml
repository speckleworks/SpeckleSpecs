get:
  tags:
    - Objects
  summary: GeometryGet
  description: Returns a geometry object via its geometry hash.
  operationId: GeometryGet
  produces:
    - application/json
  responses:
    '200':
      description: Computer says yes.
      schema:
        $ref: '#/definitions/ResponseStreamGet'
    '401':
      description: Computer says no.
      schema: 
        $ref: '#/definitions/ResponseBase'
    '404':
      description: Meep.
      schema: 
        $ref: '#/definitions/ResponseBase'
parameters:
  - name: geometryHash
    in: path
    required: true
    type: string