allOf:
  - $ref: './ResponseBase.yaml'
  - type: object
    properties:
      resource:
        $ref: './AppClient.yaml'
      resources:
        type: array
        items:
          $ref: './AppClient.yaml'    
