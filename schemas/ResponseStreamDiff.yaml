allOf:
  - $ref: './ResponseBase.yaml'
  - type: object
    properties:
      objects:
        type: object
        properties:
          common:
            type: array
            items:
              type: string
          inA:
            type: array
            items:
              type: string
          inB:
            type: array
            items:
              type: string
      layers:
        type: object
        properties:
          common:
            type: array
            items:
              $ref: './Layer.yaml'
          inA:
            type: array
            items:
              $ref: './Layer.yaml'
          inB:
            type: array
            items:
              $ref: './Layer.yaml'
