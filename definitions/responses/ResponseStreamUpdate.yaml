allOf:
  - $ref: '#/definitions/ResponseBase'
  - type: object
    properties:
      objects:
        type: array
        items:
            type: string
        description: Ordered array of the objects databaseId (_id).